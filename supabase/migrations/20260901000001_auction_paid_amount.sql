-- ============================================================================
-- Конкурс — правка механики платных мест (4+): аукцион на повышение вместо
-- фиксированной цены за место. Место определяется суммой активной ставки:
-- чем больше заплатил участник, тем выше он стоит среди мест 4+. Если кто-то
-- предлагает больше — прежний занимающий место автоматически опускается на
-- следующую позицию (и так каскадом для всех, кто ниже).
-- ============================================================================
alter table contest_entries add column if not exists paid_amount numeric;

comment on column contest_entries.paid_amount is
  'Сумма активной ставки за платное место (только для badge_type=promoted). '
  'Места 4+ пересчитываются сортировкой по этому полю по убыванию — см. resortPromotedRanks в админке.';
